

public static class ControlValues
{
    public const float viewAngle = 60;

    //This value is percentage based
    public const float speedReductionPerPlate = .15f;

    //These values are additive, not in percents
    public const float scaleIncPerPlate = 0.1f;
    public const float speedIncPerPlate = 0.25f;
    public const float powerIncPerPlate = 30;
    public const float massIncPerPlate = 3;
}
